def listaint():
    list = []
    n=int(input())
    for i in range(n):
        element = input('dati un nr de tip int: ')
     
            lst.append(int(element)
    return lst
    lista1=listaint()
    print(lista1)
